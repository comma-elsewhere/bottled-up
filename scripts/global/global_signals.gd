extends Node

signal message_opened(arg) #Triggers all message opened events, arg:int for message id in const dictionary
signal end_game(arg) #Triggers all gameover events, arg:bool is if you drowned
